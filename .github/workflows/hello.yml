name: Hello

on:
  workflow_call:
    inputs:
      docs:
        required: true
        type: string
    secrets:
      ANTHROPIC_API_KEY:
        required: true

jobs:
  doc-update:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "ok"
